services:
  python:
    build:
      context: ../../
      dockerfile: ./docker/local/Dockerfile
      args:
        ENV_NAME: myenv
    volumes:
      - ../..:/app
      - conda:/opt/conda
      - ./etc/bash_aliases:/root/.bash_aliases
    working_dir: /app

volumes:
  conda:
    driver: local
