name: myenv
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    # jupyter
    - ipykernel
    # test suite: pytest
    - pytest
    - pytest-asyncio
    - pytest-cov
